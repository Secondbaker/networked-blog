<%= form_with(model: text_block, local: true) do |form| %>
  <% if text_block.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(text_block.errors.count, "error") %> prohibited this text_block from being saved:</h2>

      <ul>
        <% text_block.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
